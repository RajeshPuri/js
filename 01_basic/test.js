const accountId = 1234
let accountEmail = 'apple@ball.com'
var accountPassword = '12356'
name='rajes'
let acountStatus
let number =null

// prefer not to use var
// because of issue in block scope and functional scope 

// accountId = 322
console.log(accountId);

console.table([accountId,accountEmail,accountPassword,name,acountStatus,number])
